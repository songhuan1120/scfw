server:
  port: 9001
spring:
#  profiles: 111
  application:
    name: micro-movie
  cloud:
    config:
      uri: http://${eureka.instance.hostname}:9006
      profile: test
eureka:
  instance:
    instanceId: ${spring.application.name}:${spring.application.instance_id:${server.port}}
    hostname: eureka1
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8000/eureka


